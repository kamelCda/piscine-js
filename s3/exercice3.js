// Exercice 3: Créer une fonction de débogage

// fonction de débogage qui affiche le resultat de la logique ci-dessous
function debug(params) {
    console.log(params);
}

// on as une logique à tester
const x=8;
const y=10;
const z=x+y;

//on lance la fonction
debug(z);
